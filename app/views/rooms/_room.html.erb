<div class="card bg-stone-300 w-96 shadow-sm">
  <figure class="h-52">
    <%= image_tag room.image %>
  </figure> 
  <div class="card-body">
    <p class="text-xl opacity-50"><%= room.name %></p>
    <p class="opacity-50"><%= room.address %></p>
    <p class="my-3 line-clamp-3 break-words"><%= room.introduction %></p>
    <p class="text-2xl text-blue-500 mb-3">¥<%= room.fee.to_s(:delimited) %>~</p>
    <% if controller.action_name == "own" %>
      <%= link_to "編集", edit_room_path(room.id), class: "btn btn-primary" %>
      <label for="delete_modal" class="btn btn-error">削除</label>
    <% else %>
      <%= link_to "詳細はこちら", room_path(room.id), class: "btn btn-primary" %>
    <% end %>
  </div>
</div>

<input type="checkbox" id="delete_modal" class="modal-toggle" />
<div class="modal" role="dialog">
  <div class="modal-box">
    <h3 class="text-lg font-bold">以下の施設を削除しますか？</h3>
    <div class="flex py-3 space-x-3">
      <div>
        <%= image_tag room.image, class: "w-20 h-16" %>
      </div>
      <div>
        <p><%= room.name %></p>
        <p>¥<%= room.fee.to_s(:delimited) %> ~ /日</p>
      </div>
    </div>
    <p>よろしければ削除ボタンを押してください</p>
    <div class="flex justify-between">
      <div class="modal-action">
        <label for="delete_modal" class="btn">キャンセル</label>
      </div>
      <%= link_to "削除", room_path(room.id), method: :delete, class: "btn btn-error mt-6" %>
    </div>
  </div>
</div>