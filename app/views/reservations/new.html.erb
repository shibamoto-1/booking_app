<div class="my-20 mx-10">
  <div class="w-1/2 mx-auto border-2 rounded-md p-5 flex flex-col space-y-5">
    <h1 class="text-2xl">予約確認</h1>
    <div class="flex justify-between">
      <p>チェックイン</p>
      <p><%= @reservation.check_in_date %></p>
    </div>
    <div class="flex justify-between">
      <p>チェックアウト</p>
      <p><%= @reservation.check_out_date %></p>
    </div>
    <div class="flex justify-between">
      <p>日数</p>
      <p><%= (@reservation.check_out_date - @reservation.check_in_date).to_i %>日</p>
    </div>
    <div class="flex justify-between">
      <p>人数</p>
      <p><%= @reservation.member_count %>人</p>
    </div>
    <div class="flex justify-between font-semibold">
      <p>お支払い金額</p>
      <p>¥<%= ((@reservation.check_out_date.to_date - @reservation.check_in_date.to_date).to_i * @reservation.room.fee * @reservation.member_count).to_s(:delimited) %></p>
    </div>
    
    <%= form_with model: @reservation do |f| %>
      <%= f.hidden_field :check_in_date %>
      <%= f.hidden_field :check_out_date %>
      <%= f.hidden_field :member_count %>
      <%= f.hidden_field :room_id %>
      <%= f.submit "予約する", class: "btn btn-primary w-1/3" %>
    <% end %>

    <%= link_to "キャンセル", :back, class: "btn w-1/3" %>
  </div>
</div>
